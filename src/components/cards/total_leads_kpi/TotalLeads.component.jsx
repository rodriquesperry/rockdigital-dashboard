import React from 'react';
import { Card } from '@/components/ui/card';

import styles from './total_leads.module.css';

const TotalLeads = () => {
	return <Card className={`${styles.container} flex flex-col card`}></Card>;
};

export default TotalLeads;
